const nomes = ["Wesley", "Emerson", "Osvaldo", "Rose", "Virginia", "Lucy"];

function aleatorio(lista){
    const posicao = Math.floor(Math.random()* lista.length);
    return lista(posicao);
}

const nome = aleatorio(nome)
